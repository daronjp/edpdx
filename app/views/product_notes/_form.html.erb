<%= form_for(@product_note) do |f| %>
  <% if @product_note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_note.errors.count, "error") %> prohibited this product_note from being saved:</h2>

      <ul>
      <% @product_note.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :product_id %><br>
    <%= f.number_field :product_id %>
  </div>
  <div class="field">
    <%= f.label :note %><br>
    <%= f.text_field :note %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
